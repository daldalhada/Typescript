const nick = "daldahada",
    age = 24,
    gender = "male";

const printing = (name , age, gender?) => {
    console.log(`nick: ${nick}, age: ${age}, gender: ${gender}`);
};

printing(nick, age);

export {};

